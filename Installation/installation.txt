This project installed some node packages

1. npm init -y
2. npm install express --save
3. npm install body-parser --save //It's middleware
4. npm install mysql --save
5. npm install --save-dev nodemon